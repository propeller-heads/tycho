# Arbitrum

Theoretically, the existing Ethereum integration [needs only few changes](https://github.com/die-herdplatte/tycho/tree/feat/fluid-arbitrum) to work on Arbitrum.
Currently, it is blocked though because Arbitrum RPCs do not yet support the required methods to enable DCI support (like `debug_storageRangeAt`).
